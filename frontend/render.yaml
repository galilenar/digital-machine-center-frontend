services:
  - type: web
    name: digital-machine-center-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_API_URL
        value: https://your-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
